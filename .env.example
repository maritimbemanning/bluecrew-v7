# =============================================================================
# BLUECREW ENVIRONMENT VARIABLES
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase (REQUIRED - database and auth)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------------------------------------------------------
# CSRF Protection (REQUIRED - forms won't work without this!)
# Generate with: openssl rand -hex 32
# -----------------------------------------------------------------------------
CSRF_SECRET=your-32-character-hex-secret-here

# -----------------------------------------------------------------------------
# Upstash Redis (REQUIRED for rate limiting and Vipps state)
# Get free tier at: https://upstash.com
# Without this, job applications will be BLOCKED in production!
# -----------------------------------------------------------------------------
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token

# -----------------------------------------------------------------------------
# Resend (REQUIRED for email notifications)
# Get API key at: https://resend.com
# -----------------------------------------------------------------------------
RESEND_API_KEY=re_your_api_key
RESEND_FROM_EMAIL=no-reply@send.bluecrew.no
RESEND_TO_EMAILS=isak@bluecrew.no,tf@bluecrew.no

# -----------------------------------------------------------------------------
# Vipps Login (REQUIRED for user authentication)
# Apply at: https://portal.vipps.no
# -----------------------------------------------------------------------------
VIPPS_CLIENT_ID=your-client-id
VIPPS_CLIENT_SECRET=your-client-secret
VIPPS_SUBSCRIPTION_KEY=your-subscription-key
VIPPS_BASE_URL=https://api.vipps.no
VIPPS_ISSUER=https://api.vipps.no/access-management-1.0/access/
VIPPS_REDIRECT_URI=https://bluecrew.no/api/vipps/callback

# JWT Session Secret (REQUIRED - minimum 32 characters!)
# Generate with: openssl rand -base64 32
VIPPS_SESSION_SECRET=your-32-character-or-longer-secret-here

# -----------------------------------------------------------------------------
# App Configuration
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=https://bluecrew.no
NEXT_PUBLIC_BASE_URL=https://bluecrew.no
